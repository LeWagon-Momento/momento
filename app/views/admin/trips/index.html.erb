<!-- TRIPS.ALL INDEX -->

<h1>ALL TRIPS <span> [ <%= Trip.count %> ]</span></h1>

<%  @trips.each do |trip| %>
 <p> <%= trip.name %> <span> [ <%= trip.id %> ] </span></p>
 <p> <%= trip.description %> </p>
 <p> <%= trip.start_date %> </p>
 <p> <%= trip.end_date %> </p>
 <button type="button" class="btn btn-primary"> <%= link_to "Delete", admin_trip_path(@trips) ,
      method: :delete,
      data: { confirm: "Are you sure?" } %> </button>
 <!-- add the edit  -->
<% end %>


